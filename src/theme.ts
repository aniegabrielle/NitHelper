import { createTheme } from "@mui/material";

export const theme = createTheme({
	palette: {
		primary: { main: "#191419", light: "#595c5b" },
		secondary: { main: "#f2f2f2", light: "#e8e6e4" },
		info: { main: "#DFAF77", dark: "#824c37" },
	},
});
